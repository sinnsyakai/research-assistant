<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>News Bot 管理画面</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --primary: #6366f1;
            --primary-dark: #4f46e5;
            --bg: #0f172a;
            --card-bg: #1e293b;
            --text: #e2e8f0;
            --text-muted: #94a3b8;
            --border: #334155;
            --success: #10b981;
            --danger: #ef4444;
        }

        body {
            background: var(--bg);
            color: var(--text);
            font-family: 'Segoe UI', system-ui, sans-serif;
            min-height: 100vh;
        }

        .container {
            max-width: 900px;
            padding: 2rem 1rem;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .header h1 {
            font-size: 1.75rem;
            font-weight: 700;
            margin: 0;
        }

        .btn-run {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            border: none;
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 12px;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .btn-run:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(99, 102, 241, 0.4);
        }

        .alert {
            background: var(--card-bg);
            border: 1px solid var(--success);
            color: var(--success);
            border-radius: 12px;
            padding: 1rem;
            margin-bottom: 1.5rem;
        }

        .alert-error {
            border-color: var(--danger);
            color: var(--danger);
        }

        .card {
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .card-title {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .genre-card {
            background: rgba(99, 102, 241, 0.1);
            border: 1px solid rgba(99, 102, 241, 0.3);
            border-radius: 12px;
            padding: 1.25rem;
            margin-bottom: 1rem;
        }

        .genre-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
        }

        .genre-name {
            font-weight: 600;
            font-size: 1.1rem;
        }

        .genre-id {
            color: var(--text-muted);
            font-size: 0.85rem;
        }

        .form-label {
            color: var(--text-muted);
            font-size: 0.85rem;
            margin-bottom: 0.5rem;
        }

        .form-control,
        .form-select {
            background: var(--bg);
            border: 1px solid var(--border);
            color: var(--text);
            border-radius: 8px;
            padding: 0.75rem;
        }

        .form-control:focus,
        .form-select:focus {
            background: var(--bg);
            border-color: var(--primary);
            color: var(--text);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2);
        }

        .form-control::placeholder {
            color: var(--text-muted);
        }

        .keywords-input {
            min-height: 80px;
            resize: vertical;
        }

        .btn-save {
            background: var(--success);
            border: none;
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            width: 100%;
            margin-top: 1rem;
        }

        .btn-save:hover {
            background: #059669;
        }

        .schedule-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        @media (max-width: 600px) {
            .schedule-grid {
                grid-template-columns: 1fr;
            }
        }

        .current-time {
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        .help-text {
            color: var(--text-muted);
            font-size: 0.8rem;
            margin-top: 0.25rem;
        }

        .number-input {
            width: 80px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>🤖 News Bot</h1>
            <form action="/run" method="post" style="margin:0;">
                <button type="submit" class="btn-run"
                    onclick="this.disabled=true; this.innerHTML='実行中...'; this.form.submit();">
                    🚀 今すぐ配信
                </button>
            </form>
        </div>

        <% if (message) { %>
            <div class="alert <%= message.includes('エラー') ? 'alert-error' : '' %>">
                <%= message %>
            </div>
            <% } %>

                <form action="/config" method="post">
                    <!-- スケジュール設定 -->
                    <div class="card">
                        <div class="card-title">⏰ 配信スケジュール</div>
                        <div class="schedule-grid">
                            <div>
                                <label class="form-label">配信時刻</label>
                                <select name="scheduleHour" class="form-select">
                                    <% for (let h=0; h < 24; h++) { %>
                                        <option value="<%= h %>" <%=(config.schedule &&
                                            config.schedule.split(' ')[1] == h) ? ' selected' : '' %>>
                                            <%= h %>:00
                                        </option>
                                        <% } %>
                                </select>
                                <div class="help-text">毎日この時刻に自動配信されます</div>
                            </div>
                            <div>
                                <label class="form-label">検索期間</label>
                                <select name="searchPeriod" class="form-select">
                                    <option value="d1" <%=config.globalSettings?.searchPeriod==='d1' ? 'selected' : ''
                                        %>>過去1日</option>
                                    <option value="d2" <%=config.globalSettings?.searchPeriod==='d2' ? 'selected' : ''
                                        %>>過去2日</option>
                                    <option value="d3" <%=config.globalSettings?.searchPeriod==='d3' ? 'selected' : ''
                                        %>>過去3日</option>
                                    <option value="w1" <%=config.globalSettings?.searchPeriod==='w1' ? 'selected' : ''
                                        %>>過去1週間</option>
                                </select>
                                <div class="help-text">何日前までのニュースを検索するか</div>
                            </div>
                        </div>
                    </div>

                    <!-- ジャンル設定 -->
                    <div class="card">
                        <div class="card-title">📰 ニュースジャンル</div>

                        <% if (config.genres && config.genres.length> 0) { %>
                            <% config.genres.forEach((genre, index)=> { %>
                                <div class="genre-card">
                                    <div class="genre-header">
                                        <div>
                                            <div class="genre-name">
                                                <%= genre.name %>
                                            </div>
                                            <div class="genre-id">ID: <%= genre.id %>
                                            </div>
                                        </div>
                                        <div>
                                            <label class="form-label" style="margin:0;">記事数</label>
                                            <input type="number" name="maxItems_<%= index %>"
                                                value="<%= genre.maxItems || 5 %>" min="1" max="20"
                                                class="form-control number-input">
                                        </div>
                                    </div>
                                    <input type="hidden" name="genreId_<%= index %>" value="<%= genre.id %>">
                                    <input type="hidden" name="genreName_<%= index %>" value="<%= genre.name %>">
                                    <label class="form-label">検索キーワード（1行に1つ）</label>
                                    <textarea name="keywords_<%= index %>" class="form-control keywords-input"
                                        placeholder="キーワードを入力..."><%= genre.keywords ? genre.keywords.join('\n') : '' %></textarea>
                                </div>
                                <% }); %>
                                    <input type="hidden" name="genreCount" value="<%= config.genres.length %>">
                                    <% } else { %>
                                        <p style="color: var(--text-muted);">ジャンルが設定されていません</p>
                                        <% } %>
                    </div>

                    <button type="submit" class="btn-save">💾 設定を保存</button>
                </form>

                <div class="card" style="margin-top: 2rem;">
                    <div class="card-title">ℹ️ ヘルプ</div>
                    <ul style="color: var(--text-muted); padding-left: 1.25rem; margin: 0;">
                        <li>「今すぐ配信」を押すと、すぐにニュースを検索してLINEに送信します</li>
                        <li>キーワードは1行に1つずつ入力してください</li>
                        <li>記事数は各ジャンルで送信する最大件数です</li>
                        <li>設定を変更したら「設定を保存」を押してください</li>
                    </ul>
                </div>

                <p class="current-time" style="text-align: center; margin-top: 2rem;">
                    サーバー時刻: <%= new Date().toLocaleString('ja-JP', {timeZone: 'Asia/Tokyo' }) %>
                </p>
    </div>
</body>

</html>